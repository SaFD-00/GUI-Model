# Core ML (versions aligned with LlamaFactory check_dependencies())
torch>=2.4.0
torchvision
torchaudio
transformers>=4.55.0,<=5.2.0
peft>=0.18.0,<=0.18.1
accelerate>=1.3.0,<=1.11.0
safetensors
datasets>=2.16.0,<=4.0.0
trl>=0.18.0,<=0.24.0

# LLaMA-Factory (install separately: pip install -e LlamaFactory/)
# deepspeed  # GPU server only
# vllm>=0.8.2  # GPU server only (inference)

# Tokenizers
sentencepiece
tiktoken

# Evaluation metrics
beautifulsoup4
munkres
nltk
rouge
jieba

# Data processing
numpy
pandas
scipy
pillow
pyarrow

# Utilities
tqdm
pyyaml
protobuf
requests
python-dotenv
